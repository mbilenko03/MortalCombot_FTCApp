package com.qualcomm.ftcrobotcontroller.opmodes.mortalcombot.pushbot.activities.PBAtomicTasks.archived;

/**
 * Created by Michael on 11/23/15.
 * Touch sensor must be present and properly mapped in pushpot class
 */


public class RunUntilTouchTask {
/*
    float speed;

    public RunUntilTouchTask(PBCommon pb, String comment, float driveSpeed){
        super(pb, comment);
        speed = driveSpeed;
    }

    @Override
    public boolean activate() {
        pushbot.setChassisPower(speed, speed);
        return pushbot.isTouchSensorPressed();
        */
    }

